// const menu = document.getElementById('navigation');
// const currentPage = location.pathname;

// menu.querySelectorAll('a').forEach(link => {
//   if (link.getAttribute('href') === currentPage) {
//     link.classList.add('navigation__link--current');
//   }
// });


// ------------trying to add listener to the input
// import { makeArrNewsForPageRead } from './read.js';
// const searchInput = document.querySelector('.search__input');
// if (window.location.pathname !== '/index.html') {
//     searchInput.addEventListener('input', makeArrNewsForPageRead)
 
// }
// const searchInputEl = document.querySelector('.search__input');
// if (window.location.pathname !== '/index.html') {
    // searchForm.removeEventListener('submit', handleSubmit);
    // searchInputEl.addEventListener('input', () => {console.log('hello')})
// }
// -------------------------------------------------------